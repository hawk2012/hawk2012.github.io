[ГЛАВНАЯ](README.md) | [ОТВЕТЫ](DEVOPS_QA.md) | [KUBERNETES](KUBERNETES.md)

## 📚 Оглавление

LINUX [Базовые утилиты, файловая система, управление процессами, сеть, безопасность](#1-linux-базовые-утилиты-файловая-система-управление-процессами-сеть-безопасность)

РАЗРАБОТКА [Системы сборки, оптимизация приложений, работа в команде разработки](#2-разработка-системы-сборки-оптимизация-приложений-работа-в-команде-разработки)

CI/CD и IaC [Поговорим о тех системах, с которыми вы работали](#3-cicd-и-iac-поговорим-о-тех-системах-с-которыми-вы-работали)

WEB-СЕРВИСЫ [Инструменты, балансировка, http/https](#4-web-сервисы-инструменты-балансировка-httphttps)

КОНТЕЙНЕРЫ и K8s [Сборка контейнеров, развертывание и администрирование приложений в K8s](#5-контейнеры-и-k8s-сборка-контейнеров-развертывание-и-администрирование-приложений-в-k8s)

БАЗЫ ДАННЫХ [Администрирование СУБД, репликация, резервное копирование, оптимизация и тюнинг](#6-базы-данных-администрирование-субд-репликация-резервное-копирование-оптимизация-и-тюнинг)

МОНИТОРИНГ И ЛОГИРОВАНИЕ [Инструменты и подходы](#7-мониторинг-и-логирование-инструменты-и-подходы)

I/O [Классификация I/O в Linux: файловый, блочный, сетевой, стандартный и другие типы](#i-o)

LOAD AVERAGE [Что такое load average и как его интерпретировать](#load-average)

#### Вопросы:
1. **Какие базовые команды Linux вы используете чаще всего?**
   - `ls`, `cd`, `pwd`, `mkdir`, `rm`, `cp`, `mv`, `cat`, `grep`, `find`, `chmod`, `chown`, `sudo`.
   
2. **Что такое файловая система Linux? Какие типы файловых систем вы знаете?**
   - Файловая система — это способ организации данных на диске. Основные типы:
     - `ext4` (распространенная файловая система Linux).
     - `XFS` (выгодна для больших файлов и высокой производительности).
     - `Btrfs` (поддерживает RAID, снапшоты и другие функции).

3. **Как можно проверить загруженность процессора и памяти в Linux?**
   - Для процессора: `top`, `htop`, `vmstat`, `mpstat`.
   - Для памяти: `free -h`, `top`, `vmstat`.

4. **Как управлять процессами в Linux?**
   - Просмотр процессов: `ps aux`, `top`, `htop`.
   - Управление процессами: `kill <PID>`, `pkill <имя_процесса>`, `killall <имя_процесса>`.

5. **Как настроить сетевое подключение в Linux?**
   - Использование `ifconfig` или `ip addr show` для просмотра интерфейсов.
   - Настройка через `/etc/network/interfaces` или NetworkManager.
   - Проверка DNS: `nslookup google.com`, `dig google.com`.

6. **Какие инструменты безопасности вы применяете в Linux?**
   - SSH для безопасного подключения (`ssh-keygen` для генерации ключей).
   - SELinux/AppArmor для контроля доступа.
   - Firewall (iptables, ufw) для управления правилами сети.
[↑ Наверх](#)
---

### **2. РАЗРАБОТКА: системы сборки, оптимизация приложений, работа в команде разработки**

#### Вопросы:
1. **Какие системы сборки вы использовали?**
   - Maven, Gradle (для Java).
   - npm, yarn (для JavaScript/Node.js).
   - Makefile, Dockerfile (для контейнеризации).

2. **Как вы оптимизируете приложения?**
   - Профилирование кода с помощью инструментов (например, `profiler` для Java, `node-prof` для Node.js).
   - Оптимизация запросов к базе данных.
   - Минификация кода и ресурсов.
   - Кэширование результатов часто используемых операций.

3. **Как организована работа в команде разработки?**
   - Использование Git для версионирования кода.
   - Принципы работы с ветками (feature branches, trunk-based development).
   - Code review перед merge'ом изменений.
   - Автоматизация тестирования и сборки.
[↑ Наверх](#)
---

### **3. CI/CD и IaC: поговорим о тех системах, с которыми вы работали**

#### Вопросы:
1. **Какие инструменты CI/CD вы использовали?**
   - Jenkins, GitLab CI, GitHub Actions, CircleCI, Travis CI.
   - Описание конвейера: от сборки до деплоя.

2. **Что такое IaC (Infrastructure as Code)? Какие инструменты вы применяли?**
   - Terraform, Ansible, Chef, Puppet.
   - Пример использования Terraform для создания инфраструктуры в AWS/Azure/GCP.

3. **Как вы автоматизировали процессы развертывания?**
   - Использование Docker для контейнеризации приложений.
   - CI/CD流水线 для автоматического тестирования и деплоя.
   - Использование Kubernetes для масштабируемости и управления контейнерами.
[↑ Наверх](#)
---

### **4. WEB-СЕРВИСЫ: инструменты, балансировка, http/https**

#### Вопросы:
1. **Какие веб-серверы вы использовали?**
   - Apache, Nginx, Caddy.
   - Особенности каждого сервера (например, Nginx лучше подходит для балансировки нагрузки).

2. **Как настроить HTTPS для веб-сервиса?**
   - Использование Let's Encrypt для получения бесплатных SSL-сертификатов.
   - Настройка виртуальных хостов в Nginx/Apache.
   - Конфигурация TLS-протокола.

3. **Как работает балансировка нагрузки? Какие инструменты вы использовали?**
   - HAProxy, Nginx, AWS ELB, GCP Load Balancer.
   - Алгоритмы балансировки: round-robin, least connections, IP hash.

4. **Какие HTTP-статусы вы знаете?**
   - 200 OK, 404 Not Found, 500 Internal Server Error, 301 Moved Permanently.
[↑ Наверх](#)
---

### **5. КОНТЕЙНЕРЫ и K8s: сборка контейнеров, развертывание и администрирование приложений в K8s**

#### Вопросы:
1. **Как создается Docker-образ?**
   - Используется Dockerfile.
   - Пример команды: `docker build -t my-image .`.

2. **Какие компоненты есть в Kubernetes?**
   - Nodes, Pods, Deployments, Services, Ingress, ConfigMaps, Secrets.

3. **Как вы развертываете приложение в Kubernetes?**
   - Создание Deployment YAML-файла.
   - Использование Helm Charts для упрощения деплоя.
   - Масштабирование приложения с помощью команды `kubectl scale`.

4. **Какие проблемы могут возникнуть при работе с Kubernetes?**
   - Нехватка ресурсов (CPU, RAM).
   - Сложности с сетью (network policies).
   - Неправильная настройка Persistent Volumes.
[↑ Наверх](#)
---

### **6. БАЗЫ ДАННЫХ: администрирование СУБД, репликация, резервное копирование, оптимизация и тюнинг**

#### Вопросы:
1. **Какие СУБД вы использовали?**
   - MySQL, PostgreSQL, MongoDB, Redis.
   - Особенности каждой СУБД (например, MySQL хорошо подходит для OLTP, а PostgreSQL — для сложных запросов).

2. **Как выполняется репликация в базах данных?**
   - Master-Slave репликация в MySQL.
   - Replication Sets в MongoDB.
   - Clustered databases (например, PostgreSQL с Citus).

3. **Как осуществляется резервное копирование баз данных?**
   - `mysqldump` для MySQL.
   - `pg_dump` для PostgreSQL.
   - Backup в облачные хранилища (AWS S3, Google Cloud Storage).

4. **Как оптимизировать запросы к базе данных?**
   - Использование индексов.
   - Оптимизация SQL-запросов (например, избегание SELECT *).
   - Разбиение таблиц (partitioning).
[↑ Наверх](#)
---

### **7. МОНИТОРИНГ И ЛОГИРОВАНИЕ: инструменты и подходы**

#### Вопросы:
1. **Какие инструменты мониторинга вы использовали?**
   - Prometheus + Grafana для метрик.
   - Nagios, Zabbix для мониторинга состояния системы.
   - Datadog, New Relic для комплексного мониторинга.

2. **Какие подходы к логированию вы применяли?**
   - Centralized logging (ELK Stack: Elasticsearch, Logstash, Kibana).
   - Fluentd для сбора логов.
   - JSON-формат для структурированных логов.

3. **Как вы определяете проблемы в системе?**
   - Анализ метрик в Prometheus.
   - Поиск ошибок в логах с помощью ELK Stack.
   - Алерты в мониторинговых системах (например, Slack/Discord уведомления).
[↑ Наверх](#)

В Linux (и в операционных системах в целом) **I/O (Input/Output)** — это процессы ввода и вывода данных между процессором или памятью компьютера и внешними устройствами. I/O охватывает любые операции, при которых данные передаются между процессом, выполняющимся в системе, и каким-либо внешним ресурсом.

### Классификация I/O в Linux:

## 🔹 1. **Файловый I/O**
Операции чтения/записи с файлами на диске.

**Примеры:**
- `open()`, `read()`, `write()`, `close()` — системные вызовы.
- Работа с файлами через команды: `cat`, `cp`, `dd`, `grep`, `echo` и т.д.
- Файлы могут быть:
  - Обычными
  - Каталогами
  - Символическими ссылками
  - Специальными файлами устройств

---

## 🔹 2. **Блочный I/O (Block I/O)**
Работа с блочными устройствами, такими как жёсткие диски, SSD, USB-накопители.

**Компоненты:**
- Драйверы устройств
- I/O планировщики (например, `cfq`, `deadline`, `noop`)
- Утилиты: `iostat`, `iotop`, `blktrace`

**Что можно измерить:**
- Время ожидания I/O
- Число запросов в секунду (IOPS)
- Пропускная способность (MB/s)

---

## 🔹 3. **Сетевой I/O**
Передача данных по сети.

**Примеры:**
- Сокеты (`socket`, `bind`, `listen`, `accept`, `send`, `recv`)
- TCP/UDP соединения
- Команды: `curl`, `wget`, `nc`, `telnet`, `ssh`
- Утилиты: `nload`, `iftop`, `iptraf`, `ss`, `netstat`

---

## 🔹 4. **Стандартный I/O (stdin/stdout/stderr)**
Это потоки ввода/вывода, доступные каждому процессу по умолчанию.

- **stdin (0)** — стандартный ввод (обычно клавиатура)
- **stdout (1)** — стандартный вывод (обычно терминал)
- **stderr (2)** — стандартный вывод ошибок

**Перенаправления в shell:**
```bash
command > output.txt     # запись stdout в файл
command < input.txt      # чтение stdin из файла
command 2> error.log     # запись stderr в файл
command >> append.txt    # добавление в файл
```

---

## 🔹 5. **Другие типы устройств I/O**
- **Консольный I/O** — взаимодействие с терминалом
- **Последовательный порт (Serial I/O)** — напр. COM-порт, UART
- **Графический I/O** — вывод на экран через DRM/KMS, X11, Wayland
- **Звуковой I/O** — воспроизведение и запись звука через ALSA, PulseAudio

---

## 🔹 6. **RAM как часть I/O**
Хотя RAM сама по себе не является устройством ввода/вывода, она тесно связана с I/O, потому что:
- Данные загружаются из диска в RAM
- Используется кэширование (page cache, buffer cache)
- Операции ввода/вывода часто происходят через буферизацию в памяти

---

## 📊 Инструменты для анализа I/O в Linux:

| Инструмент | Что делает |
|-----------|------------|
| `iostat` | Показывает статистику загрузки CPU и устройств ввода-вывода |
| `iotop` | Отображает I/O нагрузку по процессам |
| `vmstat` | Отслеживает использование памяти и swap |
| `dstat` | Многофункциональная утилита статистики системы |
| `sar` | Архивирует и отображает данные производительности |
| `strace` | Трассировка системных вызовов (включая I/O) |
| `lsof` | Показывает открытые файлы и сокеты |
| `/proc/<pid>/io` | Статистика I/O конкретного процесса |

---

## 📌 Пример: просмотр I/O для процесса
```bash
cat /proc/<PID>/io
```
Пример вывода:
```
rchar: 123456789
wchar: 987654321
syscr: 12345
syscw: 98765
read_bytes: 1234567
write_bytes: 9876543
cancelled_write_bytes: 0
```
**Load average (средняя нагрузка)** — это метрика в Linux, которая показывает среднюю загрузку системы за определённый период времени. Она отражает количество процессов, которые **находятся в состоянии выполнения или ожидания CPU** (например, ждут завершения операции ввода-вывода).

---

## 📍 Где можно увидеть load average:

1. **В командной строке:**
   ```bash
   uptime
   ```
   Пример вывода:
   ```
   12:30:45 up 10 days,  3 users,  load average: 0.15, 0.08, 0.05
   ```

2. **Или через команду `top` / `htop`:**
   В начале вывода `top` вы увидите строчку с load average.

3. **Прямо из файла `/proc/loadavg`:**
   ```bash
   cat /proc/loadavg
   ```
   Пример вывода:
   ```
   0.15 0.08 0.05 1/200 12345
   ```

---

## 🔍 Что означают три числа в load average?

Это **средние значения нагрузки за последние:**

- `0.15` — последнюю минуту  
- `0.08` — последние пять минут  
- `0.05` — последние пятнадцать минут  

Чем выше значение — тем выше нагрузка на систему.

---

## 💡 Как интерпретировать load average?

Load average сравнивается с количеством **ядер CPU** (или потоков, если используется Hyper-Threading):

| Load Average | Смысл |
|--------------|-------|
| `< 1.0` | Нагрузка ниже возможностей процессора (хорошо) |
| `≈ 1.0` | Процессор полностью занят |
| `> 1.0` | Система перегружена — процессы ожидают доступа к CPU |

**Пример для машины с 4 ядрами:**
- Load 4.0 = полная загрузка CPU
- Load 5.0 = часть задач ждёт освобождения CPU

---

## ⚠️ Когда стоит беспокоиться?

Если load average **постоянно превышает количество ядер CPU**, это может означать:

- Высокая CPU-нагрузка (`CPU-bound`)
- Блокировки на дисковые операции (`I/O-bound`)
- Зациклившиеся процессы
- Утечки ресурсов или баги в приложениях

---

## 🛠️ Инструменты для анализа высокой нагрузки:

| Команда | Описание |
|--------|----------|
| `top`, `htop` | Отслеживает активные процессы и использование CPU |
| `mpstat` | Показывает статистику по каждому ядру |
| `vmstat` | Статистика по памяти, I/O, контекстным переключениям |
| `iostat` | Анализ дискового I/O |
| `pidstat` | Статистика по конкретным процессам |
| `sar` | История нагрузки (если установлен sysstat) |

---

## ✅ Пример интерпретации:

```
load average: 6.0, 4.5, 3.0
```
на сервере с 4 ядрами означает:

- За последнюю минуту система была **перегружена на 50%** (6.0 / 4.0 = 1.5)
- За 5 минут — нагрузка снижалась
- За 15 минут — ещё больше снижалась

---

## 📌 Полезный совет:

Load average — это **системная метрика**, но она сама по себе не говорит о причине нагрузки. Для диагностики нужно использовать другие инструменты (`top`, `iostat`, `iotop`, `vmstat` и т.д.).

---

Если хочешь, могу помочь разобрать конкретную ситуацию с load average на твоём сервере.
